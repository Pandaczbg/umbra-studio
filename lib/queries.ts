/**
 * UMBRA STUDIO — V6
 * Legacy query compatibility facade
 *
 * Canonical query logic lives in:
 * @/lib/content/queries
 *
 * This module intentionally contains no query logic of its own.
 * It exists only to preserve existing imports while the codebase
 * migrates completely to the V6 content architecture.
 */

export * from "@/lib/content/queries";